# Images

Put your portrait here as `profile.jpg` (square crop works best, at least 400×400 px).
The site shows a placeholder until the file exists.

To use a different filename, change `profile.photo` in `content.js` or in the admin editor.
